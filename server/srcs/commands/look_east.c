/*
** EPITECH PROJECT, 2018
** Project Name
** File description:
** zappy
*/

#include "server.h"

void	look_east(server_t *server, client_t *client, char *str)
{
	int	posX;

	dprintf(client->fd, "[");
	// if (client->posX == server->parse->)
}
